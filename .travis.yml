# gokaygurcan/gokaygurcan.github.io

language: node_js

node_js:
  - '6'

notifications:
  email: false

before_install:
  - npm install -g npm
  - npm install -g xo
  - npm install -g yarn
  - npm install -g babel-cli flow-bin jest nodemon

before_script:
  - node --version
  - npm --version
  - yarn --version

script: 
  - npm test
